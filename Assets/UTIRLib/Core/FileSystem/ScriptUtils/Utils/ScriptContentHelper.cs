#nullable enable
namespace UTIRLib.FileSystem.ScriptUtils
{
    public static class ScriptContentHelper
    {
        //private static (Type type, int priority)[] DefaultPriorities {
        //    get => new (Type type, int priority)[] {
        //        (typeof(IField), 0),
        //        (typeof(IMethod), 1),
        //        (typeof(IType), 1)
        //    };
        //}

        ///// <exception cref="ArgumentNullException"></exception>
        //public static IScriptContent SortByType(IType typeEntry,
        //                                        params (Type type, int priority)[] priorities)
        //{
        //    if (typeEntry is null)
        //        throw new ArgumentNullException(nameof(typeEntry));

            
        //}
    }
}
