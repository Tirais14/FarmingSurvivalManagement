#nullable enable
namespace Game.Core
{
    public static class FactoryExtensions
    {
        //public static TCreated? Create<TCreated>(this IFactory<string, TCreated> factory, Enum nameEnum) =>
        //    factory.Create(nameEnum.ToString());
        //public static TComponent? Create<TComponent>(this GameObjectFactory factory, Enum nameEnum)
        //    where TComponent : Component => factory.Create<TComponent>(nameEnum.ToString());
        //public static TCreated? Create<TCreated, TArg>(this IFactory<string, TArg, TCreated> factory, Enum nameEnum,
        //    TArg arg) => factory.Create(nameEnum.ToString(), arg);
        //public static TComponent? Create<TCreated, TArg, TComponent>(this IFactory<string, TArg, TCreated> factory, 
        //    Enum nameEnum, TArg arg)
        //    where TComponent : Component => factory.Create(nameEnum.ToString(), arg).;
    }
}
